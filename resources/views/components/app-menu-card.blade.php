{{-- <div> --}}
    <!-- No surplus words or unnecessary actions. - Marcus Aurelius -->
    <div class="col-md-5">
        <div class="card mb-4 py-3">
            <div class="card-body text-center py-1">
                <a href="{{$link ?? '#'}}">
                    <img width="120" src="{{ $icon ?? '' }}" alt="{{$alt ?? ''}}">
                    <h6 class="card-title">
                        {{$title}}
                    </h6>
                </a>
            </div>
        </div>
    </div>
{{-- </div> --}}